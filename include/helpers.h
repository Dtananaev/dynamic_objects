#ifndef DYNAMIC_OBJECTS_HELPRERS_H
#define DYNAMIC_OBJECTS_HELPRERS_H

#include <vector>
#include "data_types.h"

namespace dynamic_objects
{
    class Helpers
    {

    public:
        static void get_color_palette(std::vector<dynamic_objects::color> &colors);

    private:
    };
} // namespace dynamic_objects

#endif // DYNAMIC_OBJECTS_HELPRERS_H